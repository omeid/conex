name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go:
          - "1.21" # Minimum required version (from go.mod)
          - "1.23" # Latest stable
          - "master"

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go == 'master' && 'stable' || matrix.go }}
          check-latest: ${{ matrix.go == 'master' }}

      - name: Get boxes
        run: ./ci/get_boxes.sh

      - name: Run tests
        run: go test -v -race ./...
